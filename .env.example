# LinuxServer user/group identifiers
PUID=1000
PGID=1000

# Timezone
TZ=Etc/UTC

# KasmVNC web UI password (optional)
PASSWORD=password

# Extra APT packages to install at container startup (space-separated)
# Example: EXTRA_PACKAGES="golang-go ruby rustc cargo postgresql-client"
EXTRA_PACKAGES=
